---
minor_changes:
  - New module samba_ntacl to set the NT ACL, owner and group of a share root and of managed folders (created if
    missing) through the share's VFS stack on the file server, and to pass inheritance and optionally ownership on
    to the other files and folders (C(propagate), C(propagate_owner)).
