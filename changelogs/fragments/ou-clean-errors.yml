---
bugfixes:
  - >-
    samba_ou_info - an invalid C(path) is rejected with a clear message instead
    of a traceback; the value is parsed as a distinguished name before it is
    used as the search base.
  - >-
    samba_ou - deleting an OU the directory protects (systemFlags) or the bind
    user may not delete is reported cleanly, with the directory's reason,
    instead of as a raw ldb error.
